services:
  - type: web
    name: ai-invoice-processor-backend
    runtime: python
    buildCommand: apt-get install -y tesseract-ocr tesseract-ocr-eng && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TESSDATA_PREFIX
        value: /usr/share/tesseract-ocr/4.00/tessdata
```

Then on Render dashboard:
1. Go to your service → **Settings**
2. Find **"Build Command"** field
3. Manually type this directly in that field:
```
apt-get install -y tesseract-ocr tesseract-ocr-eng && pip install -r requirements.txt
